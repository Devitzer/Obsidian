# In this file, you will see usually the newest features or an empty file.

declare add (x, y) {
    let result = x + y
    io.print(result)

    result
}

static result = add(10, 4)

io.print(result)